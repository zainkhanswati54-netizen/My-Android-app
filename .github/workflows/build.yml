# ============================================================
#  Titan Studio PRO - GitHub Actions Build Workflow
#  Version: 14.0.3 - FULL LOG VERSION
# ============================================================

name: Build Titan Studio PRO APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    name: Build APK (Docker)
    runs-on: ubuntu-22.04

    steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build APK using Docker
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/home/user/hostcwd \
            -e ANDROID_SDK_ROOT=/home/user/.android/sdk \
            kivy/buildozer:latest \
            bash -c "cd /home/user/hostcwd && buildozer android debug 2>&1 | tee build_output.txt; exit \${PIPESTATUS[0]}" || true

      - name: Show last 100 lines of log
        if: always()
        run: |
          echo "====== LAST 100 LINES OF BUILD LOG ======"
          tail -100 build_output.txt 2>/dev/null || echo "No log file found"

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build_output.txt
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: TitanStudioPRO-v13-debug
          path: bin/*.apk
          retention-days: 30
          if-no-files-found: error
