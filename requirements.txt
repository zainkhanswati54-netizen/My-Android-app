# =============================================================
#  Titan AI Studio Pro — requirements.txt  v10.1
#  Install: pip install -r requirements.txt
# =============================================================

# ── Core UI ───────────────────────────────────────────────────
kivy==2.2.1

# ── Text-to-Speech (PRIMARY - Microsoft Neural TTS, 100% FREE) ─
edge-tts>=6.1.9        # Real male/female voices, speed, pitch, volume
aiohttp>=3.9.0         # Required by edge-tts (async HTTP)

# ── Text-to-Speech (FALLBACK - basic, no gender/pitch support) ─
gTTS==2.5.1

# ── HTTP / Network ────────────────────────────────────────────
requests==2.31.0
certifi==2024.2.2
chardet==5.2.0
idna==3.6
urllib3==2.2.1

# ── Image processing ──────────────────────────────────────────
Pillow==10.2.0

# ── Build tools ───────────────────────────────────────────────
setuptools>=68.0.0
