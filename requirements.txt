# =============================================================
#  Titan AI Studio Pro — requirements.txt  v1.1.0  (Fixed)
#  Install: pip install -r requirements.txt
# =============================================================

# ── Core UI ───────────────────────────────────────────────────
kivy==2.2.1

# ── Text-to-Speech (Android compatible — NO asyncio) ──────────
# edge-tts REMOVED: uses asyncio which crashes on Android
gTTS==2.5.1

# ── HTTP / Network (ElevenLabs API) ───────────────────────────
requests==2.31.0
certifi==2024.2.2
chardet==5.2.0
idna==3.6
urllib3==2.2.1

# ── Image processing ──────────────────────────────────────────
Pillow==10.2.0

# ── Build tools ───────────────────────────────────────────────
setuptools>=68.0.0
