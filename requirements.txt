# ============================================================
#  Titan Studio PRO - requirements.txt
#  Version 2.0.0  |  Kokoro TTS Edition
# ============================================================
#
#  INSTALL (PC/dev environment):
#    pip install -r requirements.txt
#
#  NOTE: For Android build, dependencies are listed in
#        buildozer.spec under [app] requirements = ...
#        This file is for your development environment only.
#
#  KOKORO MODELS (auto-downloaded on first run):
#    Kokoro downloads voice models (~80MB) on first use.
#    Make sure internet is available the first time.
#    After that, English TTS works fully OFFLINE.
# ============================================================

# ── UI Framework ────────────────────────────────────────────
kivy==2.3.0

# ── TTS Engine 1: Kokoro Neural TTS (PRIMARY) ───────────────
# Offline, high-quality neural TTS engine.
# True Male/Female voices for English.
# Kokoro voice IDs: am_michael (Male), af_heart (Female)
# Speed, pitch, emotion all properly supported.
kokoro>=0.9.4

# ── Audio I/O (required by Kokoro) ──────────────────────────
soundfile>=0.12.1
sounddevice>=0.4.6

# ── NumPy (required by Kokoro for audio arrays) ─────────────
numpy>=1.24.0

# ── TTS Engine 2: gTTS (FALLBACK for non-English) ───────────
# Used for Urdu, Hindi, Arabic, French, etc.
# Requires internet connection.
gTTS>=2.5.1

# ── HTTP / Networking (required by gTTS) ─────────────────────
requests>=2.31.0
urllib3>=2.0.0
certifi>=2024.2.2
charset-normalizer>=3.3.0
idna>=3.6

# ── Buildozer (for building APK) ────────────────────────────
# Install separately:
# pip install buildozer
